use crate::lexer::token_stream::TokenStream;

use super::{parser::FileAst, ast_node::ParsingResult};

//pub(in crate::parser) fn parse(stream: &mut TokenStream, file_ast: &mut FileAst) -> ParsingResult {
//    
//}